# 安全策略

## 支持哪些版本

| 版本 | 支持状态 |
|---|---|
| 最新发布 tag（见 [CHANGELOG.md](CHANGELOG.md)） | ✅ 接受安全问题报告与修复 |
| 更早的 tag | ⚠️ 只在维护者有时间时回溯修复，不承诺 |
| `master` | ❌ 不作为发布渠道；它是随时可能含中间状态的开发线 |

**只随 tag 发布修复**。因此生产部署请固定到某个 tag 而不是跟踪 `master` ——
理由写在这里：数据库迁移向前不可逆，跟踪开发线意味着某次升级会把你的库推到
一个没有对应代码回退路径的状态上。

## 报告一个安全问题

**请不要开公开 Issue，也不要在群里贴复现步骤。** 本项目曾把一份逐条列出缺陷与
修法的审计文档直接提交进公开仓库，结果是：即便后来把它从历史里抹掉，那些细节
在被公开的时间窗内已经无法收回。这份经历决定的现在的流程 —— 细节先私下走，
修完并发版之后，再把可公开的部分写进 CHANGELOG。

1. **首选：GitHub 私有漏洞报告** —— 在 <https://github.com/motao123/dwz-shorturl/security/advisories/new>
   提交。该入口需要维护者在仓库 *Settings → Security → Private vulnerability reporting*
   里开启；若页面提示未开启，请走第 2 步。
2. **兜底：**在本仓库开一个**只写一句"安全问题，请私下联系"的空 Issue**（不要写
   组件名、不要写复现步骤、不要写版本号细节），并在评论里 @ 维护者 `motao123`，
   由维护者主动通过 Issue 私信或另建渠道与你接续。
3. 有条件的话请附：**受影响版本号**、触发所需的**最小配置**（是否 Docker、是否单库
   模式、是否配了 Redis），以及修复验证方式。复现代码可以后补，不必写在 Issue 里。

## 处理节奏

维护者是单人业余维护，**不承诺响应时限**，但可以承诺两件事：

- 收到后会先确认"是否已修复、影响哪个版本区间"，再讨论修复；
- 修复发版后，会在 CHANGELOG 里以**能力分组**的方式披露（例如"鉴权与会话"），
  而不是逐条列出可利用路径。需要更多技术细节时，请在修复确认后向维护者索取。

## 加固类变更会带来的行为变化（部署前值得知道）

这些是有意的取舍，不是 bug，写在这里免得被当成回归：

- **配了 Redis 却连不上时，后台接口会返回 503**（不是放行）。登出、禁用账号、改密、
  改角色的吊销记录都存在 Redis 里；"读不到就当没吊销"等于把吊销存储打挂即可绕过。
  没有配置 Redis 的部署不受影响，此时该能力按未启用处理。
- **禁用账号、改角色、重置密码、删号会立刻作废该账号已签发的全部会话**，
  在线用户会被要求重新登录。
- **会员 API 校验同源**，非本站来源的跨域调用会被拒绝。
- **`stats.export` 权限已在 v1.0.0 移除**。它曾存在于权限表但没有任何接口校验它，
  留着等于后台里一个"勾了不起作用"的开关。将来若实现统计导出，会连同接口一起加回。

## 如果敏感文档已经进了公开仓库

本文档开头提到的这次泄露，彻底清理**不是**"删掉文件再提交一次"就结束了。
git 的历史、开放中的合并请求快照都还指向旧内容，只删工作区文件等于没删。
下面是实际走过一次的完整处置顺序（本地能做的与只能由平台做的分开列）。

**先确认暴露面，别凭感觉判断。** 只要有一个 ref 还指着旧提交，
匿名用户一条命令就能取到全文：

```bash
git init . && git remote add origin <仓库地址>
git fetch --depth 1 origin refs/pull/<编号>/head
git cat-file -p FETCH_HEAD:path/to/secret.md | wc -l   # 非 0 即仍可读
```

**本地能做的（维护者自己就能完成）：**

1. `git filter-repo` 重写全部 `refs/heads/*` 与 `refs/tags/*`，把文件从每一次提交里拔掉；
2. 强推分支与 tag，并逐条确认重写后各分支的最终 SHA 是预期值——**不要**顺手重新发布 tag，
   已发布的 tag 被重写会让下游的校验失败；
3. 把文件名模式补进 `.gitignore` 与 `.cnb.yml` 的「内部文档入库检查」门禁，
   防止同类文档再次进入版本管理。

**只能由平台侧做的（客户端与 CI 凭据都做不到，已实测穷尽）：**

4. 删除承载旧快照的 `refs/pull/*`（每个合并请求有 `head` / `merge` / `status` 三条）；
   `refs/pull/*` 是平台保护的命名空间，普通分支接口一律 `not found`，
   连推送/删除 hook 也直接 `hook declined`；
5. 在仓库后端执行 `git gc --prune=now`。**这一步不能省**——删除 ref 只是让对象不可达，
   不 GC 的话 `git cat-file -e <旧SHA>:<路径>` 依然命中；
6. 判定标准以"匿名 fetch 取不到"为准，而不是"页面上看不见了"。

**两点易踩的坑：**

- 只有**确实含该文件的那几个 PR**需要处理，不要按"PR 全量删除"去发起操作，
  那会连带清掉仍在被引用的历史，收益为零还有反作用；
- 转私有仓库只是临时止血，一旦公开暴露面就原样恢复。

### 转交平台管理员的话术

请求里要写清三件事，否则容易被当成普通的"删分支"工单退回：
受控文件的**文件名模式**、承载它的确切的 **ref 列表（含 `status`）**、
以及**删 ref 后必须执行 GC**。附上上面那段匿名 `cat-file` 的复现输出作为依据。
