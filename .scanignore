# ============================================================
# CNB 代码安全扫描 · 公共忽略规则
#
# gitignore 风格路径规则，默认对 secrets 与 SCA 两类能力生效。
# 目的：把「确定无攻击面」的路径排除出扫描范围，避免存量噪音
#       淹没真实问题（详见 docs/SECURITY_SCAN.md）。
# ============================================================

# ---------- 第三方代码与依赖产物 ----------
vendor/
**/vendor/
node_modules/
**/node_modules/

# ---------- 构建产物 ----------
dist/
**/dist/
**/*.min.js
**/*.min.css

# ---------- 测试装置 ----------
**/testdata/**
**/*_test.go

# ---------- 示例配置（占位符非真实密钥）----------
config.sample.php
backend/configs/config.example.yaml
**/*.example.yaml
**/*.sample.*

# ---------- 文档与静态站（无运行时代码）----------
# 内部文档（审计/分析/验收类）不入库，敏感词真源见 deploy/internal_docs_pattern；
# 这里只需排除仓库内可能存在的文档目录与静态站。
# 注意：本文件是"扫描范围"而非"入库门禁"，放宽它不会让敏感文档进仓库，
# 入库由 .gitignore + deploy/check_internal_docs.sh 把关。
docs/
**/docs/
site/
