# 更新日志

遵循 [SemVer](https://semver.org/lang/zh-CN/)。**只发布 tag**，`master` 不是发布渠道。
安全修复的披露方式见 [SECURITY.md](SECURITY.md)：按能力分组说明，不逐条列出可利用路径。

## v1.0.0 — 2026-09-21

首个正式发布。此前该项目没有建立发布线（零 tag），部署方只能跟踪 `master` ——
本次把当前已验证的主干定为 v1.0.0，此后升级以 tag 为准。

### 鉴权与会话

- 管理台与会话生命周期收口：登录凭据分型（刷新令牌不再能当访问令牌用）、登出即作废、
  禁用账号/改角色/重置密码/删号**即时**作废已签发会话。
- 吊销存储不可用时按"拒绝"处理（503），不再降级为"当作没吊销"。
- 会员侧令牌走 HttpOnly cookie，并新增 API 同源校验。
- Cookie 与跳转地址不再写死站点根路径，子目录部署可用。

### 权限与归属

- 内置角色 `admin` / `operator` / `viewer` 有了最小权限种子（旧版本里它们一条权限都没有，
  受限账号登录后处处 403，实际迫使所有人用超管）。
- 权限位真正参与校验；API Key 具备 scope 与归属约束，跨账号读取/吊销他人密钥被堵住。
- 低权限账号不能再通过"改更高权限账号的密码/2FA"实现提权。
- 移除 `stats.export` 这条无接口校验的死权限。

### 限流、配额与风控

- 限流键统一命名空间并区分故障态；密码保护短链的尝试次数按 IP+短码限流。
- 会员建链总量上限（可后台调整）、批量操作上限由服务端下发，前端不再静默截断。
- 违规域名/关键词规则支持不重编译生效；云元数据地址与内网目标在建链阶段即被拒。

### 部署与运维

- 迁移体系单一事实来源，新增迁移文件无需改代码；迁移加分布式锁避免并发踩踏。
- 一键部署与备份/恢复脚本可用并纳入 CI 演练；`deploy/restore.sh` 提供恢复路径。
- 后台日志支持 JSON 格式输出以对接采集；健康探针区分"未配置/掉线/正常"三态。
- 敏感配置读取失败不再静默降级为放行。

### 后台与前台体验

- 列表页分页/筛选/批量/导出契约统一，CSV 导出与列表共用同一套筛选解析（此前两者可能不一致）。
- 修复"编辑管理员会静默清空其角色"这一自毁式交互。
- 暗色适配、对比度、键盘可达性等无障碍问题成批修正；受密码保护页跟随系统主题。

### 升级注意

1. 迁移只向前：打上本版本迁移的库**不要**退回更早代码运行。
2. 配了 Redis 的部署，在 Redis 不可达期间后台接口会返回 503（有意的安全取向）。
3. 生产请固定 tag：`git clone --branch v1.0.0` 或 `git checkout v1.0.0`，步骤见 README「版本与升级」。

### 本版本没有做的事

如实记录，避免被当作已覆盖：

- 未做 docker-compose 全栈端到端冒烟（维护机器无 docker 环境）；容器路径由 CI 静态门禁 +
  分步演练覆盖，不等于一次真实起栈验证。
- 与真实 Redis / MySQL 交互的行为（分布式锁竞争、吊销水位线读写、级联删除）在 CI 中不被覆盖，
  需要部署方在自己的环境里确认。
- 少量性能与数据规模相关项在缺乏真实数据基线前刻意不动，理由见 SECURITY.md 与代码内注释。
