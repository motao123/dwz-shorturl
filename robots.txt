# 说明：robots.txt 仅约束合规爬虫，对攻击者无约束力；因此这里不再罗列
# stats.php / includes/ / migrations/ 等敏感路径（罗列本身会泄露路径存在性，
# 反而形成信息收集入口）。敏感路径应由服务端返回 404/403 处理，
# 详见 nginx.example.conf 的 deny 段。
User-agent: *
Allow: /
Disallow: /admin/

# Sitemap 指令按协议必须是绝对地址，而这个文件是静态下发的、不知道自己在哪个域上，
# 所以不能再硬编码上游维护者的站点（那会把爬虫和抓取配额送到别人的域名去）(#20)。
# 部署时把下面这行的注释去掉，换成你自己的域名：
# Sitemap: https://your-domain.com/sitemap.xml
